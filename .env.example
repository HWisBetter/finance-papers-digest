# 用法：把这个文件复制成 .env，再把下面的值换成你真实的 key。
#   PowerShell:  Copy-Item .env.example .env
#
# 注意：.env 已被 .gitignore 忽略，绝对不要提交到 git（里面是真实密钥）。
# .env.example 这个模板本身不含真实密钥，可以放心提交，方便以后别人知道要配哪些变量。

DEEPSEEK_API_KEY=your_key_here

# Elsevier 官方 Abstract Retrieval API（给 JFE 等 Elsevier 期刊补摘要的合规正路）。
# 在 https://dev.elsevier.com 免费注册即可拿到 APIKey。【不填也能跑】——
# 留空时自动跳过 Elsevier 这一层，回退到 Semantic Scholar，流水线照常工作。
ELSEVIER_API_KEY=

# 仅当某些订阅内容的摘要被机构权限挡住时才需要：向你单位图书馆申请的
# Institutional Token（X-ELS-Insttoken）。一般补摘要用不到，留空即可。
ELSEVIER_INST_TOKEN=
